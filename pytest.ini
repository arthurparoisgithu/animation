[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q

# Ajoute la racine du projet au sys.path, sinon « import app » echoue.
# « python -m pytest » ajoute le dossier courant automatiquement, mais
# « pytest » tout court — la commande utilisee par la CI — ne le fait pas.
pythonpath = .
